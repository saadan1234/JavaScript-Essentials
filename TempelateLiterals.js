// Can integrate javascript inside string.
const variable = 100.
const summary = `This is ${variable} percent working well.`

// Always use backtics.
summary;

// Split Function
// varaible.split("splitter i.e. (-), (:) etc)[array index]
console.log(summary.split(" ")[1]);

//Ternary Operator
//const value = (condition)? True Code : False Code ;  